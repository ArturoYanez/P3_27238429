<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bienvenida</title>
  <style>
    /* Estilos generales */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    /* Contenedor principal */
    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background-color: #f2f2f2;
    }

    /* Título */
    .title {
      font-size: 36px;
      font-weight: bold;
      margin-bottom: 20px;
    }

    /* Descripción */
    .description {
      font-size: 18px;
      margin-bottom: 40px;
    }

    /* Botón */
    .button {
      display: inline-block;
      padding: 10px 20px;
      font-size: 16px;
      font-weight: bold;
      text-decoration: none;
      color: #fff;
      background-color: #007bff;
      border-radius: 4px;
    }

    /* Estilos responsivos */
    @media (max-width: 768px) {
      .title {
        font-size: 24px;
      }

      .description {
        font-size: 50px;
        font-weight: bold;
        margin-bottom: 10px;
      }

      .button {
        font-size: 14px;
        padding: 8px 16px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="title">¡Bienvenido!</h1>
    <p class="description">Arturo Yanez</p>
    <p class="description">Cedula: 27238429</p>
    <p class="description">Seccion: 3</p>
    <a href="#" class="button">Comenzar</a>
  </div>
</body>
</html>